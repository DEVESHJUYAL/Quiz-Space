server.port=${PORT:8080}
spring.application.name=quiz-space

# ?? PostgreSQL ????????????????????????????????????????????????
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? Schema initialisation ?????????????????????????????????????
# Spring Boot runs schema.sql FIRST (before Hibernate, before any bean).
# This guarantees all tables exist before @EventListener methods fire.
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# ?? JPA / Hibernate ???????????????????????????????????????????
# validate = Hibernate checks columns match entities but does NOT create/alter tables.
# schema.sql handles all DDL, so Hibernate never needs to touch the schema.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# Defer datasource init so schema.sql runs before Hibernate validates
spring.jpa.defer-datasource-initialization=true

# ?? JWT ??????????????????????????????????????????????????????
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ?? Cloudinary ???????????????????????????????????????????????
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# ?? File uploads ??????????????????????????????????????????????
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ?? CORS ?????????????????????????????????????????????????????
cors.allowed-origins=${CORS_ALLOWED_ORIGINS}

# ?? Default Admin Account ?????????????????????????????????????
admin.email=${ADMIN_EMAIL:admin@quizspace.app}
admin.password=${ADMIN_PASSWORD:Admin@123}
admin.name=${ADMIN_NAME:Quiz Space Admin}

# ?? Suppress noisy warnings ???????????????????????????????????
spring.jpa.open-in-view=false